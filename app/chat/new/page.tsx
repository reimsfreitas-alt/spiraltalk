"use client";
import {useRouter,useSearchParams} from "next/navigation";import {useEffect,useState} from "react";
export default function NewChat(){const r=useRouter();const q=useSearchParams();const [topic]=useState(q.get("topic")||`topic-${Date.now()}`);useEffect(()=>{fetch("/api/session/create",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({topicKey:topic})}).then(x=>x.json()).then(d=>{if(d.sessionId)r.replace(`/chat/${d.sessionId}`)})},[topic,r]);return <main className="min-h-screen flex items-center justify-center p-6"><p className="text-zinc-400">Abrindo conversa…</p></main>}
