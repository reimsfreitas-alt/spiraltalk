"use client";
import {useEffect,useRef} from "react";
type Star={x:number;y:number;r:number;a:number;p:number};type Meteor={x:number;y:number;vx:number;vy:number;life:number;max:number;len:number};
export default function NightSky(){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const c=ref.current;if(!c)return;const x=c.getContext("2d");if(!x)return;let w=0,h=0,d=1,raf=0,last=performance.now(),stars:Star[]=[],ms:Meteor[]=[];let next=performance.now()+28000+Math.random()*30000;const build=()=>{d=Math.min(devicePixelRatio||1,2);w=c.clientWidth;h=c.clientHeight;c.width=w*d;c.height=h*d;x.setTransform(d,0,0,d,0,0);stars=Array.from({length:Math.min(520,Math.floor(w*h*.00022))},()=>({x:Math.random()*w,y:Math.random()*h,r:.35+Math.random()*1.1,a:.18+Math.random()*.45,p:Math.random()*6.28}))};const meteor=()=>{const left=Math.random()>.5,s=.22+Math.random()*.16,a=(18+Math.random()*14)*Math.PI/180;ms.push({x:left?-40:w+40,y:Math.random()*h*.42,vx:(left?1:-1)*Math.cos(a)*s,vy:Math.sin(a)*s,life:0,max:1000,len:80+Math.random()*80})};const draw=(now:number)=>{const dt=Math.min(now-last,64);last=now;x.clearRect(0,0,w,h);for(const s of stars){s.x+=dt*.0005;if(s.x>w)s.x=-2;x.beginPath();x.arc(s.x,s.y,s.r,0,Math.PI*2);x.fillStyle=`rgba(232,230,225,${s.a.toFixed(3)})`;x.fill()}if(now>=next){meteor();next=now+28000+Math.random()*34000}ms=ms.filter(m=>{m.life+=dt;m.x+=m.vx*dt;m.y+=m.vy*dt;const t=m.life/m.max;if(t>=1)return false;const nx=m.x-m.vx*m.len,ny=m.y-m.vy*m.len,g=x.createLinearGradient(m.x,m.y,nx,ny);g.addColorStop(0,"rgba(255,255,255,.5)");g.addColorStop(1,"rgba(255,255,255,0)");x.strokeStyle=g;x.beginPath();x.moveTo(m.x,m.y);x.lineTo(nx,ny);x.stroke();return true});raf=requestAnimationFrame(draw)};build();raf=requestAnimationFrame(draw);const r=()=>build();addEventListener("resize",r);return()=>{cancelAnimationFrame(raf);removeEventListener("resize",r)}},[]);return <canvas ref={ref} className="night-sky"/>}
